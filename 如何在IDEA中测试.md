# 在 IntelliJ IDEA 中测试插件

## 步骤一:配置 IntelliJ Platform SDK

1. **打开项目结构**
   - 点击 `File > Project Structure` (或按 `Ctrl+Alt+Shift+S`)

2. **添加 IntelliJ Platform Plugin SDK**
   - 在左侧选择 `SDKs`
   - 点击 `+` 号
   - 选择 `Add IntelliJ Platform Plugin SDK`
   - 选择您的 IntelliJ IDEA 安装目录
     - 例如: `C:\Program Files\JetBrains\IntelliJ IDEA 2025.3.1`
   - SDK 名称可以保持默认,例如 `IntelliJ IDEA IC-253.x`

3. **配置项目 SDK**
   - 在左侧选择 `Project`
   - 在 `SDK` 下拉框中选择刚才添加的 IntelliJ Platform Plugin SDK
   - `Language level` 选择 `17 - Sealed types, always-strict floating-point semantics`

4. **配置模块**
   - 在左侧选择 `Modules`
   - 选择您的模块 `health-reminder-plugin`
   - 在 `Dependencies` 标签页中
   - 确保 `Module SDK` 使用项目 SDK

5. **点击 `OK` 保存配置**

## 步骤二:创建运行配置

1. **打开运行配置**
   - 点击 `Run > Edit Configurations...`

2. **添加新的 Plugin 配置**
   - 点击左上角 `+` 号
   - 选择 `Plugin`
   - 名称输入: `Run Health Reminder Plugin`

3. **配置参数** (通常保持默认即可)
   - `Use classpath of module`: 选择 `health-reminder-plugin`
   - `JRE`: 使用项目 SDK

4. **点击 `OK` 保存**

## 步骤三:运行插件

1. **启动插件**
   - 点击工具栏的运行按钮 (绿色三角形)
   - 或按 `Shift+F10`
   - 或点击 `Run > Run 'Run Health Reminder Plugin'`

2. **等待沙箱 IDE 启动**
   - 会打开一个新的 IntelliJ IDEA 窗口 (沙箱环境)
   - 这个窗口中已经安装了您的插件

## 步骤四:测试插件功能

### 测试 1: 检查插件是否加载

在沙箱 IDE 中:
1. 打开 `File > Settings > Plugins`
2. 在 `Installed` 标签页搜索 "Health Reminder"
3. 应该能看到您的插件已启用

### 测试 2: 配置提醒

1. 打开 `File > Settings > Tools > Health Reminder`
2. 查看默认的两个提醒:
   - 喝水提醒 (30分钟)
   - 休息眼睛 (20分钟)
3. 尝试:
   - 点击 "添加" 创建新提醒
   - 点击 "编辑" 修改提醒间隔 (建议改为 1 分钟方便测试)
   - 选择不同的提醒方式
   - 点击 "Apply" 保存

### 测试 3: 等待提醒触发

1. **将提醒间隔改为 1 分钟**以便快速测试
2. 等待 1 分钟,应该会看到提醒弹窗
3. 测试按钮:
   - 点击 "确定" - 提醒关闭,继续等待下次提醒
   - 点击 "稍后提醒(5分钟)" - 5分钟后会再次提醒

### 测试 4: 查看统计

1. 打开 `View > Tool Windows > Health Statistics`
2. 查看:
   - 今日总计
   - 喝水次数
   - 休息次数
   - 历史记录表格

### 测试 5: 状态栏功能

1. 查看 IDE 右下角状态栏
2. 应该能看到 "⏱ 下次提醒: XX:XX" 的倒计时
3. 点击状态栏图标可以暂停/恢复提醒

### 测试 6: 暂停/恢复

1. 在设置页面点击 "暂停提醒"
2. 状态栏应显示 "⏸ 提醒已暂停"
3. 不会再收到提醒
4. 点击 "恢复提醒" 重新开始

## 常见问题

### Q: 找不到 "Plugin" 运行配置选项?
A: 确保已安装 "Plugin DevKit" 插件:
   - `File > Settings > Plugins`
   - 搜索 "Plugin DevKit"
   - 如果未安装,点击 "Install" 安装

### Q: 运行时报错 "Cannot find IntelliJ IDEA installation"?
A: 需要正确配置 IntelliJ Platform SDK,指向您的 IDEA 安装目录

### Q: 沙箱 IDE 启动很慢?
A: 这是正常的,第一次启动会比较慢,后续会快一些

### Q: 修改代码后如何重新测试?
A: 
   1. 关闭沙箱 IDE
   2. 在主 IDE 中点击 "Run" 重新启动
   3. 或使用 "Debug" 模式可以热重载部分代码

## 调试技巧

### 使用 Debug 模式
1. 点击 `Run > Debug 'Run Health Reminder Plugin'`
2. 可以在代码中设置断点
3. 当触发提醒时会暂停在断点处

### 查看日志
1. 在沙箱 IDE 中打开 `Help > Show Log in Explorer`
2. 查看 `idea.log` 文件
3. 搜索 "ReminderService" 可以看到插件的日志输出

### 清除测试数据
插件的配置和统计数据保存在:
- Windows: `C:\Users\<用户名>\.IdeaIC<版本>\config\options\`
- 文件: `healthReminderSettings.xml` 和 `healthReminderStatistics.xml`
- 删除这些文件可以重置插件状态

## 下一步

测试通过后,您可以:
1. **打包插件**: 使用 `Build > Prepare Plugin Module for Deployment`
2. **安装到真实 IDE**: 在真实的 IntelliJ IDEA 中安装测试
3. **发布到 JetBrains Marketplace**: 分享给其他开发者

祝测试顺利! 🎉


.\gradlew.bat clean
.\gradlew.bat runIde --refresh-dependencies